// p2pai-coordinator: bootstrap libp2p, relay, registro de nós, stats e créditos.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "p2pai-coordinator: esqueleto. Ver docs/PROPOSTA.md, seção 3.")
	os.Exit(1)
}
